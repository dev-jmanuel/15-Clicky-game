import React from "react";
import "./style.css";

function Navbar() {
    return (
        <nav className="navbar navbar-default navbar-fixed-top">
            <div className="instructions">"Click every employee to win, but  <u><font color="#c5bfb5">don't</font></u> click them more than once" -Wayne Gretzky" -Michael Scott</div>
        </nav>

    );
}

export default Navbar;
